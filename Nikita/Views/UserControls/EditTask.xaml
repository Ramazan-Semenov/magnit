<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Nikita.Views.UserControls"
             xmlns:Nikita="clr-namespace:Nikita" x:Class="Nikita.Views.UserControls.EditTask"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        
        <TextBox Text="{Binding Task_Book.Numder}" Height="30" Margin="75,42,529,378" />
        <TextBox Text="{Binding Task_Book.Id}" Height="30" Margin="76,103,528,317"/>
        <TextBox Text="{Binding Task_Book.Id}" Height="30" Margin="78,146,526,274"/>
        <TextBox Text="{Binding Task_Book.Id}" Height="30" Margin="75,190,529,230"/>


    </Grid>
</UserControl>
